import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mynetworkingagent.app',
  appName: 'My Networking Agent',
  webDir: 'dist'
};

export default config;
