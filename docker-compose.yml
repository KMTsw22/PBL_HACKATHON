version: '3.8'

services:
  # 내 웹 앱 서비스 이름
  web-app:
    build: .             # 현재 폴더의 Dockerfile로 빌드
    ports:
      - "5173:5173"      # [내 컴퓨터 포트]:[컨테이너 포트]
    volumes:
      - .:/app           # 내 컴퓨터의 소스 코드를 실시간으로 연결 (수정 시 바로 반영)
      - /app/node_modules # 노드 모듈은 컨테이너 안의 것을 사용